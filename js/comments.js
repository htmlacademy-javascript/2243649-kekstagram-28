/* eslint-disable no-undef */
/* eslint-disable no-shadow */
/* eslint-disable no-unused-vars */
/*const SHOW_COMMENT = 5;
const commentsContainer = bigPicture.querySelector('.social__comments');
const elementLi = commentsContainer.querySelector('li');

//Комментарии
const createComments = (current, commentsContainer, elementLi) => {
  const commentFragment = document.createDocumentFragment();
  const dataComments = current.comments;
  commentsContainer.innerHTML = '';
  dataComments.forEach(({avatar, message, name}) => {
    const commentsCounter = elementLi.cloneNode(true);
    commentsCounter.querySelector('.social__picture').src = avatar;
    commentsCounter.querySelector('.social__picture').alt = name;
    commentsCounter.querySelector('.social__text').textContent = message;
    commentFragment.append(commentsCounter);
  });
  commentsContainer.append(commentFragment);
};

*/
